define(["jQuery", "jQuery.UI"], function(require, exports, module) {
    var $ = require("jQuery"),
        ui = require("jQuery.UI");

    $.fn.slider = function(){
        alert("slider");
    };
});